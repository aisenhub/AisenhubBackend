# Documentation Archive

This archive is intentionally **curated**, not exhaustive.

It preserves superseded architecture/plan context needed to understand historical decisions. Routine implementation evidence is intentionally removed from the live tree.

Exact deleted originals remain recoverable from Git history using the source commit/path/SHA manifests.

Archived content is never an implementation authority.
